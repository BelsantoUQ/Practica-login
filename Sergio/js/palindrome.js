'use strict'

/*Dado un número entero x, devuelve verdadero si x es un palíndromo y falso en caso contrario.*/

var numero_validar=prompt('Ingrese un número para validar si es palíndrome', 0);
var num_cadena = numero_validar.split('');
var mostrar = isPalindrome(num_cadena);
var num_reves=[];
limite= num_cadena.length();
console.log(num_reves);



function isPalindrome(x){
    for(var i=0; i<x.length; i++){
       num_reves(i)= num_cadena(limite);
       limite = limite -1;
    }
}

var numero1= parseInt(prompt('Ingrese un número:',0));
var numero2= parseInt(prompt('Ingrese un número:',0));

function sumPrimos(x,y){
    if (numero1=numero2) {
        console.log('No es posible realizar la suma el numero 2 debe ser mayor al numero 1')    
    }
    else{
            for(var i=x; i<y;i++){

            }
    }
}

